package cad.lab;


public class Nomer31 {
    public static void main(String[] args) {
        int number = 0;

        System.out.println("number\tsqrt\tcube");
        System.out.printf("%d\t\t%d\t\t%d%n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d%n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

        number = number + 1;
        System.out.printf("%d\t\t%d\t\t%d\n", number, number * number, number * number * number);

    }
}
